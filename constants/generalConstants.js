// General
const TOKEN = 'Authorization';
const LOGIN = 'croquignolex';
const PASSWORD = 'croquignolex';

// Date format
const JS_DATE_FORMAT = 'DD-MM-YYYY HH:mm';

module.exports = {
    TOKEN,
    LOGIN,
    PASSWORD,
    JS_DATE_FORMAT
}
