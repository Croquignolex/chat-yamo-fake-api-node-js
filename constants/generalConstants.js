// General
const TOKEN = 'Authorization';

// Date format
const JS_DATE_FORMAT = 'DD-MM-YYYY HH:mm';

// Cases status
const OPEN = 'OPEN';
const CLOSE = 'CLOSE';

module.exports = {
    OPEN,
    TOKEN,
    CLOSE,
    JS_DATE_FORMAT
}
